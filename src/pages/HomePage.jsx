import * as React from 'react';
import Typography from '@mui/material/Typography';

export const HomePage = () => (
  <Typography variant="h3" gutterBottom>
    If you want to encrypt your messages, you need to register
  </Typography>
);
